function Button(props) {
    return (
        // <button onClick={props.handleBtnClick(3,4)} style={{fontSize: 20}}>
        <button onClick={props.handleBtnClick} style={{fontSize: 20}}>
            d(^_^)b
        </button>
    );
}

export default Button;
